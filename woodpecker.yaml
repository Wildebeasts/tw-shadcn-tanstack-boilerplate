steps:
  - name: frontend
    image: bash
    commands:
      - node -v
      - pnpm install
      - pnpm build
      - pm2 delete BJ_FE
      - pm2 start serve --name BJ_FE -- -s ./Bean-Journal-Web-FE/dist/ -l 3000
